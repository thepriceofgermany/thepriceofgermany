{
  "name": "thepriceofgermany",
  "version": "1.0.0",
  "private": true,
  "description": "Companion static site for The Price of Germany YouTube channel",
  "type": "module",
  "scripts": {
    "new-post": "node scripts/new-post.mjs",
    "studio": "node scripts/studio.mjs",
    "sitemap": "node scripts/build-sitemap.mjs",
    "data": "node scripts/build-cost-data.mjs",
    "calculator": "node scripts/build-calculator.mjs",
    "preview": "npx serve site",
    "deploy": "npm run calculator && npm run sitemap && npx wrangler pages deploy site --project-name thepriceofgermany --branch main --commit-dirty=true"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0"
  }
}
